version: 0.2

phases:
  install:
    runtime-versions:
      nodejs: 20
    commands:
      - npm ci
  pre_build:
    commands:
      - npx tsc --noEmit
  build:
    commands:
      - npm run build

artifacts:
  base-directory: out
  files:
    - '**/*'